{
  "id": "harness-memory",
  "name": "Harness memory recall",
  "description": "Recalls what this deployment remembers and prepends its structure to a turn's context.",
  "kind": "memory",
  "activation": {
    "onStartup": true,
    "onCapabilities": [
      "hook"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "read": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "title": "Reader argv",
        "description": "The reader to spawn, as argv, up to and including the `bundle` subcommand. Written by harnesses/openclaw/install-memory.sh. An argv that is empty, or that names a read shape other than `bundle`, recalls nothing and says so in the log rather than injecting silence."
      },
      "threadEntity": {
        "type": "string",
        "title": "Thread entity kind",
        "description": "The entity kind this deployment files conversations under, as its spec/entities.yaml declares it. Set it and a turn inside a thread looks that thread up; leave it unset and no thread is asked about. There is no default, because an entity vocabulary belongs to the deployment rather than to this harness."
      },
      "actors": {
        "type": "object",
        "additionalProperties": {
          "type": "string"
        },
        "title": "Agent id to writer name",
        "description": "This deployment's map from the host's agent id to the writer name that agent's records were filed under \u2014 for example {\"main\": \"main_bot\"}. The two are named separately on purpose: a record carries whatever caller its record socket signed as, so the writer name belongs to the keyring and this plugin cannot derive it from an agent id. An agent this map does not name asks about no actor at all, and says so in the log; passing the agent id instead would ask about a writer that never wrote anything and return a page indistinguishable from a quiet store."
      },
      "specDir": {
        "type": "string",
        "title": "Entity spec directory",
        "description": "The deployment's spec directory, holding entities.yaml and extractors.yaml. Set it and the turn's message is read for entity references, which are looked up alongside the thread and the actor. Read-time inference only: a key guessed wrongly matches nothing, and the confidence a stored reference needs to reach a bundle is untouched."
      },
      "timeoutMs": {
        "type": "integer",
        "exclusiveMinimum": 0,
        "title": "Recall budget (ms)",
        "description": "How long the whole lookup gets. It sits in front of a reply, so a slow read is a slow conversation: when it runs out the turn proceeds with no recalled context."
      },
      "maxRecords": {
        "type": "integer",
        "exclusiveMinimum": 0,
        "title": "Records injected",
        "description": "Most records one turn may carry. Also the reader's `--limit`, so a turn that wants eight records is charged for eight rather than for the service's cap."
      },
      "maxChars": {
        "type": "integer",
        "exclusiveMinimum": 0,
        "title": "Characters injected",
        "description": "Ceiling on the rendered block. Whatever it cuts is counted in the block, because a truncated list that reads as complete is worse than a short one."
      },
      "searchFallback": {
        "type": "boolean",
        "title": "Search when the bundle matched nothing",
        "description": "A bundle that matched nothing is followed by one ranked search over the same socket, injected under a heading saying it is the weaker claim. Set it false for a deployment that wants only what its keys support. Declared here because the host refuses a config key this schema does not name: an undeclared setting is not a default, it is a config the gateway will not load."
      },
      "digestDays": {
        "type": "integer",
        "exclusiveMinimum": 0,
        "title": "Session-opening digest window (days)",
        "description": "Days of recent activity a session wakes up holding, as a date-grouped digest. Injected on the turn that opens a session, and only where the bundle and the search both matched nothing, so recall keeps the space whenever it has an answer. Unset is off, and off is the default: a digest says who acted, when, and what they referenced, and never what any of it was about — whether that reads as background or as noise depends on a store this harness has not seen."
      },
      "digestMaxRecords": {
        "type": "integer",
        "exclusiveMinimum": 0,
        "title": "Digest rows",
        "description": "Most rows a digest may carry, and the window read's `--limit`. Wider than maxRecords by default, because a digest is a list rather than an answer."
      },
      "digestMaxChars": {
        "type": "integer",
        "exclusiveMinimum": 0,
        "title": "Digest characters",
        "description": "Ceiling on the rendered digest, and it belongs below maxChars. A window read returns a window rather than a match, so it is the likelier of the two to be cut — and whatever it cuts is counted in the block."
      }
    }
  }
}
